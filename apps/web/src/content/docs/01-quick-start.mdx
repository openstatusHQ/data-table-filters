---
title: "Quick Start"
description: "From an empty shadcn project to a rendered, filterable table: one prerequisite, one install command, one paste"
author: "openstatus"
publishedAt: "2026-03-12"
faq:
  - question: "How do I install data-table-filters?"
    answer: "In any shadcn project, run `npx shadcn@latest add https://data-table.openstatus.dev/r/data-table.json https://data-table.openstatus.dev/r/data-table-schema.json`. That installs the core table engine, the memory store adapter, all 4 filter types, and the schema system with DataTableAuto. Then render `<DataTableAuto data={rows} />`."
  - question: "How do I create a new project with data-table-filters already installed?"
    answer: "Run `npx shadcn@latest init https://data-table.openstatus.dev/r/data-table-example-infinite.json --name my-app --template next -p nova`. The shadcn CLI creates the Next.js app, initializes shadcn on Base UI, and installs a working example route with every block it needs. Run `npm run dev` and open http://localhost:3000/example. Add `-b radix` before `-p nova` for Radix."
  - question: "What are the prerequisites?"
    answer: "A React project with Tailwind CSS v4 and shadcn/ui initialized — either library. `npx shadcn@latest init -d` (Base UI, the CLI default) and `npx shadcn@latest init -b radix -p nova` (Radix) both work. The CLI handles all dependency resolution automatically."
  - question: "Does data-table-filters work with Base UI?"
    answer: "Yes, including the shadcn CLI default (`init -d`, style `base-nova`). CI installs the Quick Start into a Base UI project and a Radix project on every change and typechecks both. If you installed before Base UI support landed and see errors such as `Property 'delayDuration' does not exist`, re-add the blocks with `--overwrite`."
---

# Quick Start

Three steps: a shadcn project, one install command, one paste.
Measured on a clean machine, the whole path from `create-next-app` to a green
`next build` takes about 30 seconds.

## 1. Prerequisite

> **Prerequisite.** Works on either shadcn library: the CLI default, Base UI (`npx shadcn@latest init -d`), or Radix (`npx shadcn@latest init -b radix -p nova`). CI installs into both and typechecks them on every registry change and nightly.

Starting from nothing? One command creates the Next.js app, initializes
shadcn, and installs a working example with every block it needs:

```bash
npx shadcn@latest init https://data-table.openstatus.dev/r/data-table-example-infinite.json --name my-app --template next -p nova
cd my-app
npm run dev
```

Open [localhost:3000/example](http://localhost:3000/example): a table over
5,000 mock rows with filters, facet counts, infinite scroll, a command
palette and a row sheet. It lives in `app/example` — the table schema, a
mock API route, and the page — so change the columns in
`app/example/table-schema.ts` and watch every surface follow, or delete the
folder once you have your own table. That lands on Base UI; for Radix, add
`-b radix` before `-p nova`. Steps 2 and 3 below are how to add a table of
your own.

Already have a shadcn project? Nothing to change: any `"style"` in
`components.json` works — `base-…` (Base UI), `radix-…`, or the pre-v4
`new-york` / `default`. The blocks read the primitives from whichever one you
are on.

## 2. Install

One command installs the core `data-table` block (table engine, memory store
adapter, all 4 filter types) and `data-table-schema` (the schema system,
including `DataTableAuto`):

```bash
npx shadcn@latest add https://data-table.openstatus.dev/r/data-table.json https://data-table.openstatus.dev/r/data-table-schema.json
```

The CLI resolves the shadcn primitives the blocks need, rewrites `@/` import
paths to match your `components.json`, and injects the CSS variables.

## 3. Render

Paste this into a page — `app/page.tsx`, or `src/app/page.tsx` if the project
uses a `src` directory:

```tsx
import { DataTableAuto } from "@/components/data-table/data-table-auto";

const data = [
  {
    name: "Alice",
    role: "admin",
    rating: 5,
    created_at: "2026-01-15T10:00:00Z",
  },
  { name: "Bob", role: "user", rating: 3, created_at: "2026-02-20T14:30:00Z" },
];

export default function Page() {
  return <DataTableAuto data={data} />;
}
```

Columns, filters, display types, and cell renderers are auto-inferred from your
data, and the command palette and row detail sheet come along. See the
[/auto](/auto) example for a live demo.

![data-table with some filters and activated live mode](/assets/docs/data-table.png)

## You own the files

There is no `data-table-filters` package in your `package.json` — only the
blocks' own dependencies (TanStack Table, cmdk, and friends). The blocks landed
as source under `src/components/data-table/` and `src/lib/`. Edit them like any
other file in your repo. When the inferred table is not enough, [eject to the
Builder](/builder) or define a [Table Schema](/docs/table-schema) by hand.

## Agent Skill

Install the plugin in Claude Code:

```bash
/plugin marketplace add openstatushq/data-table-filters
/plugin install data-table-filters@openstatus
```

Or install the skill with any agent that supports the `skills` CLI:

```bash
npx skills add https://github.com/openstatushq/data-table-filters --skill data-table-filters
```

Let the agent handle the rest:

> Add a data table with filters to my project

It detects your stack, installs the right blocks, and wires them up. For agents without the skill, see
[For AI Agents](/docs/agents) — `llms.txt`, raw markdown docs, and Cursor rules.

## Blocks

Every block installs with the same command:

```bash
npx shadcn@latest add https://data-table.openstatus.dev/r/<block>.json
```

| Block                          | Install URL                                                                                                | What it adds                                                                   |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `data-table`                   | [data-table.json](https://data-table.openstatus.dev/r/data-table.json)                                     | Core: table engine, store, 4 filter types, memory adapter                      |
| `data-table-schema`            | [data-table-schema.json](https://data-table.openstatus.dev/r/data-table-schema.json)                       | Declarative schema system with `col.*` factories and `DataTableAuto`           |
| `data-table-cell`              | [data-table-cell.json](https://data-table.openstatus.dev/r/data-table-cell.json)                           | Cell renderers: badges, bars, heatmaps, status codes, timestamps               |
| `data-table-sheet`             | [data-table-sheet.json](https://data-table.openstatus.dev/r/data-table-sheet.json)                         | Row detail side panel                                                          |
| `data-table-filter-command`    | [data-table-filter-command.json](https://data-table.openstatus.dev/r/data-table-filter-command.json)       | Command palette with history and keyboard shortcuts                            |
| `data-table-nuqs`              | [data-table-nuqs.json](https://data-table.openstatus.dev/r/data-table-nuqs.json)                           | nuqs URL state adapter                                                         |
| `data-table-zustand`           | [data-table-zustand.json](https://data-table.openstatus.dev/r/data-table-zustand.json)                     | zustand state adapter                                                          |
| `data-table-drizzle`           | [data-table-drizzle.json](https://data-table.openstatus.dev/r/data-table-drizzle.json)                     | Drizzle ORM server-side helpers                                                |
| `data-table-query`             | [data-table-query.json](https://data-table.openstatus.dev/r/data-table-query.json)                         | React Query infinite query integration                                         |
| `data-table-filter-command-ai` | [data-table-filter-command-ai.json](https://data-table.openstatus.dev/r/data-table-filter-command-ai.json) | Natural language → filter inference                                            |
| `data-table-mcp`               | [data-table-mcp.json](https://data-table.openstatus.dev/r/data-table-mcp.json)                             | MCP server endpoint for AI agents                                              |
| `data-table-actions`           | [data-table-actions.json](https://data-table.openstatus.dev/r/data-table-actions.json)                     | Row and bulk actions rendered from server metadata                             |
| `data-table-remote`            | [data-table-remote.json](https://data-table.openstatus.dev/r/data-table-remote.json)                       | Headless table that renders from an API endpoint's manifest                    |
| `data-table-example-infinite`  | [data-table-example-infinite.json](https://data-table.openstatus.dev/r/data-table-example-infinite.json)   | Ready-to-run `/example` route: schema, mock API, infinite table with URL state |

> Looking to add natural language filtering? The `data-table-filter-command-ai` block translates queries like _"5xx errors last 24h"_ into structured filters using any LLM. See [AI Filters](/docs/ai-filters) for setup.

## Next Steps

- Browse the [/infinite](/infinite) demo to see all components in action
- Learn about the [Table Schema](/docs/table-schema) builder for type-safe column definitions
- Explore the [UI Components](/docs/ui-components) available out of the box
- Rows live in Postgres? Follow [Drizzle ORM](/docs/drizzle-orm) for server-side filtering, facets, and cursor pagination
